query PullRequestQuery($owner: String!, $name: String!) {
  repository(owner: $owner, name: $name) {
    pullRequests(first: 1, states: OPEN) {
      nodes {
        title
        createdAt
        closedAt
        url
      }
    }
  }
}
